name: Sync Fork with Upstream

on:
  schedule:
    - cron: '0 */3 * * *'
  workflow_dispatch:
  push:
    branches:
      - master
    paths-ignore:
      - '.github/workflows/**'   # 修改 workflow 文件不会触发运行

jobs:
  sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
          ref: master

      - name: Configure Git
        run: |
          git config user.name "GitHub Actions Bot"
          git config user.email "actions@github.com"
          git config pull.rebase false

      - name: Add upstream repository
        run: |
          if ! git remote | grep -q upstream; then
            git remote add upstream https://github.com/LmeSzinc/AzurLaneAutoScript.git
          fi
          git remote -v

      - name: Fetch upstream changes
        run: |
          git fetch upstream master

      - name: Check if merge is needed
        id: check
        run: |
          git log HEAD..upstream/master --oneline > commits.txt
          if [ -s commits.txt ]; then
            echo "有新的提交需要同步"
            echo "new_commits=true" >> $GITHUB_OUTPUT
            echo "commit_count=$(wc -l < commits.txt)" >> $GITHUB_OUTPUT
            echo "commits=$(cat commits.txt | head -5 | tr '\n' '; ')" >> $GITHUB_OUTPUT
            cat commits.txt
          else
            echo "没有新的提交"
            echo "new_commits=false" >> $GITHUB_OUTPUT
          fi

      - name: Merge upstream changes
        if: steps.check.outputs.new_commits == 'true'
        id: merge
        run: |
          echo "CONFLICT_DETECTED=false" >> $GITHUB_ENV
          
          if git merge upstream/master --no-edit; then
            echo "✅ 合并成功，无冲突"
            # 合并已自动创建提交，无需额外操作
          else
            echo "❌ 检测到冲突，合并失败"
            echo "CONFLICT_DETECTED=true" >> $GITHUB_ENV
            
            conflict_files=$(git diff --name-only --diff-filter=U)
            echo "冲突文件列表:" > conflict_files.txt
            echo "$conflict_files" >> conflict_files.txt
            
            git merge --abort
          fi

      - name: Send conflict notification
        if: steps.check.outputs.new_commits == 'true' && env.CONFLICT_DETECTED == 'true'
        env:
          REPO: ${{ github.repository }}
          COMMIT_COUNT: ${{ steps.check.outputs.commit_count }}
        run: |
          conflict_list=$(cat conflict_files.txt)
          
          # 使用 jq 构建 JSON payload
          payload=$(jq -n \
            --arg repo "$REPO" \
            --arg time "$(date '+%Y-%m-%d %H:%M:%S')" \
            --arg count "$COMMIT_COUNT" \
            --arg conflicts "$conflict_list" \
            '{
              body: ("仓库: " + $repo + "\n冲突时间: " + $time + "\n上游提交数: " + $count + "\n\n冲突文件:\n" + $conflicts + "\n\n需要手动解决冲突！\n请访问: https://github.com/" + $repo + "/actions"),
              title: "🚨 GitHub 同步冲突",
              group: "github",
              url: ("https://github.com/" + $repo + "/actions")
            }')
          
          curl -s -X POST -H "Content-Type: application/json" -d "$payload" "https://api.day.app/N7xUCURtR3EWhsotV8oAUD/"
          
          echo "已发送冲突通知"

      - name: Push successful merge
        if: steps.check.outputs.new_commits == 'true' && env.CONFLICT_DETECTED == 'false'
        run: |
          git push origin master
          
          echo "## ✅ 同步成功" >> $GITHUB_STEP_SUMMARY
          echo "已成功同步上游更新到你的仓库" >> $GITHUB_STEP_SUMMARY
          echo "📊 同步了 ${{ steps.check.outputs.commit_count }} 个提交" >> $GITHUB_STEP_SUMMARY
          echo "📝 最近的提交：" >> $GITHUB_STEP_SUMMARY
          echo "${{ steps.check.outputs.commits }}" | sed 's/; /\n- /g' | sed 's/^/- /' >> $GITHUB_STEP_SUMMARY

      - name: Create summary for conflicts
        if: steps.check.outputs.new_commits == 'true' && env.CONFLICT_DETECTED == 'true'
        run: |
          echo "## 🚨 同步失败 - 检测到冲突" >> $GITHUB_STEP_SUMMARY
          echo "同步过程中检测到冲突，需要手动解决。" >> $GITHUB_STEP_SUMMARY
          echo "📊 尝试同步 ${{ steps.check.outputs.commit_count }} 个提交" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 冲突文件：" >> $GITHUB_STEP_SUMMARY
          cat conflict_files.txt >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 解决方法：" >> $GITHUB_STEP_SUMMARY
          echo "1. 克隆仓库到本地" >> $GITHUB_STEP_SUMMARY
          echo "2. 执行以下命令：" >> $GITHUB_STEP_SUMMARY
          echo '   ```bash' >> $GITHUB_STEP_SUMMARY
          echo '   git fetch upstream' >> $GITHUB_STEP_SUMMARY
          echo '   git merge upstream/master' >> $GITHUB_STEP_SUMMARY
          echo '   ```' >> $GITHUB_STEP_SUMMARY
          echo "3. 手动解决冲突" >> $GITHUB_STEP_SUMMARY
          echo "4. 提交并推送到GitHub" >> $GITHUB_STEP_SUMMARY
